# White Paper Summary

컨설팅 회사들의 White Paper를 AI로 요약하여 한눈에 파악할 수 있는 웹 애플리케이션입니다.

## 주요 기능

- 📄 **MD 파일 업로드**: AI로 요약한 마크다운 파일을 업로드
- 📁 **폴더 스캔**: 지정된 폴더 구조에 파일을 넣고 자동 인덱싱
- 🔍 **필터링**: 년도별, 주제별, 회사별 필터링
- 📊 **통계 대시보드**: 차트로 분포 현황 파악
- 🎨 **마크다운 렌더링**: 요약 내용을 예쁘게 표시

## 기술 스택

### 백엔드
- Python FastAPI
- SQLite + SQLAlchemy
- Pydantic

### 프론트엔드
- React 18 + TypeScript
- Vite
- TailwindCSS
- React Router
- Recharts

## 시작하기

### 1. 백엔드 실행

```bash
cd backend

# 가상환경 생성 (권장)
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Mac/Linux

# 패키지 설치
pip install -r requirements.txt

# 서버 실행
uvicorn app.main:app --reload
```

백엔드가 http://localhost:8000 에서 실행됩니다.
API 문서: http://localhost:8000/docs

### 2. 프론트엔드 실행

```bash
cd frontend

# 패키지 설치
npm install

# 개발 서버 실행
npm run dev
```

프론트엔드가 http://localhost:5173 에서 실행됩니다.

## 사용 방법

### 방법 1: 웹에서 업로드

1. http://localhost:5173/upload 접속
2. MD 파일 선택
3. 제목, 회사, 년도, 주제 입력
4. 업로드 버튼 클릭

### 방법 2: 폴더 스캔

1. `backend/storage/papers/` 폴더에 다음 구조로 파일 저장:
   ```
   storage/papers/
   ├── 2024/
   │   ├── AI/
   │   │   └── mckinsey-generative-ai-report.md
   │   └── Digital/
   │       └── bcg-digital-transformation.md
   └── 2023/
       └── Strategy/
           └── bain-growth-strategy.md
   ```
   
   파일명 형식: `회사명-제목.md`

2. 홈페이지에서 "스캔" 버튼 클릭

## AI 요약 팁

원본 White Paper를 ChatGPT, Claude 등에게 요약 요청할 때:

```
다음 리포트의 핵심 내용을 마크다운 형식으로 요약해줘.

포함할 내용:
- 리포트 개요 (1-2문장)
- 주요 발견 사항 (3-5개)
- 핵심 인사이트
- 시사점 및 권장 사항
- 주요 수치/통계

마크다운 헤딩과 불렛 포인트를 사용해서 가독성 있게 정리해줘.
```

## 프로젝트 구조

```
consultingSummary/
├── frontend/               # React 프론트엔드
│   ├── src/
│   │   ├── components/     # UI 컴포넌트
│   │   ├── pages/          # 페이지
│   │   ├── api/            # API 클라이언트
│   │   └── types/          # TypeScript 타입
│   └── package.json
├── backend/                # FastAPI 백엔드
│   ├── app/
│   │   ├── routers/        # API 라우터
│   │   ├── models.py       # DB 모델
│   │   ├── schemas.py      # Pydantic 스키마
│   │   └── main.py         # 앱 진입점
│   ├── storage/papers/     # MD 파일 저장소
│   └── requirements.txt
└── README.md
```

## API 엔드포인트

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | /api/papers | 목록 조회 (필터링) |
| GET | /api/papers/{id} | 상세 조회 |
| GET | /api/papers/filters | 필터 옵션 조회 |
| GET | /api/papers/stats | 통계 데이터 |
| POST | /api/papers/upload | 파일 업로드 |
| POST | /api/papers/scan | 폴더 스캔 |
| DELETE | /api/papers/{id} | 삭제 |

